type Query {
    likesForPic(picId: ID): LikesForPic
}

type LikesForPic {
    picId: String
    likes: [Like]
}

type Like {
    likeId: ID!
    userId: String
    likeComment: String
    likeCount: Int
}